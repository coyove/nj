#set fib, a, b = 0,2
#fib = lambda(n)
#    if n < 2 then return n end
#    return fib(n-2)+fib(n-1)
#end
#
#set ans = fib(15)
#
#out_println(ans ,a,b)

set l = list(list(1,2,3),list(4,5,6),list(7,8,9)) 
set idx = 1
set a = foreach
a(l, lambda(i,x)
    foreach(x, lambda(i,x)
        assert idx == x
        idx=idx+1
    end)
end)