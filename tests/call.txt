function endsWith(s, e)
if len(s) < 3 then return false end
    return s(-3, len(s)) == e
end

http.handle("/", function (q)
    if endsWith(q, "zzz") then
        return core.bytes("hello zzz")
    else
        return core.bytes("helloooooo " + q)
    end
end)
http.listen(":8888")